import store from '../../redux/reduxStore';
import {TOKEN} from '@env';

// export const ACCESS_TOKEN = store.getState().authPage.auth.accessToken;
export const ACCESS_TOKEN = TOKEN;
export const API_VERSION = '5.81';
