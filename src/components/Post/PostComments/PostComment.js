import React from 'react';
import {View} from 'react-native';
import styles from './PostCommentStyles';

export const PostComment = props => {
  return (
    <View style={styles.container}>
    </View>
  );
};
